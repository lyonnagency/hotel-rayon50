$(document).ready(function () {
    $('.slider-custom').slick({
        // dots: true,
        infinite: true,
        // speed: 300,
        // slidesToShow: 2,
        // centerMode: true,
        arrows: true,
        variableWidth: true
    });
});